import 'package:flutter/material.dart';

class PlantTypesTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Types of Plants Content'),
    );
  }
}
